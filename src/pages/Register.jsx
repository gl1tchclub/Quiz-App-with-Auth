/**
 * @file Register.jsx
 * @module Register
 * @description Page component for rendering the RegisterForm.
 * @author Elizabeth Minty
 */

import RegisterForm from "../components/forms/RegisterForm";

/**
 * RegisterPage component renders the RegisterForm component.
 * @returns {JSX.Element} Rendered component displaying the registration form.
 */
const RegisterPage = () => {
  return <RegisterForm />;
};
export default RegisterPage;
